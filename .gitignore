# macOS
.DS_Store

# Editor swap/backup files
*.swp
*.swo
*~

# Never track secrets, local overrides, or backup config files
*.secrets
*.secrets.*
*.local
*.bak

# opencode's own node/bun artifacts (it has its own .gitignore too, kept as defense-in-depth)
opencode/.config/opencode/node_modules
